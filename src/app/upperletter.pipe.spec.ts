import { UpperletterPipe } from './upperletter.pipe';

describe('UpperletterPipe', () => {
  it('create an instance', () => {
    const pipe = new UpperletterPipe();
    expect(pipe).toBeTruthy();
  });
});
